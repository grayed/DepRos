namespace DepRos
{
    /// <summary>
    /// Destination toolkit being used for dependency property generation.
    /// </summary>
    internal enum Toolkit {
        Unknown = 0,
        Avalonia,
        Uwp,
        WinUI,
        Wpf,
    }
}
